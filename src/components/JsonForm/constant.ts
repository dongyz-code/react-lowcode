import { Input, Select, InputNumber, DatePicker, Checkbox, Radio, Switch, TreeSelect } from 'antd';

export const JsonFormItemType = {
  input: Input,
  select: Select,
  inputNumber: InputNumber,
  datePicker: DatePicker,
  checkbox: Checkbox,
  radio: Radio,
  switch: Switch,
  treeSelect: TreeSelect,
};
