import { FC, useState } from 'react';
import type { IHoverMaskProps } from './interface';

const HoverMask: FC<IHoverMaskProps> = () => {
  const [position, setPosition] = useState({
    
  })
  return <></>;
};

export default HoverMask;
